# Reserved iOS runtime

`codex-agent-runtime-ios` is the reserved name for a future iOS runtime module.
No placeholder implementation or artifact exists. A future runtime must
implement the existing `CodexRuntimeFactory` contract from `codex-agent-client`
without changing the client to an `expect`/`actual` factory.
